# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python
*.egg-info/
dist/
build/
.eggs/

# Jupyter
.ipynb_checkpoints/
*.ipynb_checkpoints

# Virtual environments
venv/
.venv/
env/
.env

# IDE
.vscode/
.idea/

# MLflow local tracking store (experiment metadata/artifacts, not source)
mlruns/
mlflow.db
mlartifacts/

# Retrain supervisor state (host-local pid + wall-clock timestamps, not source).
# results/retrain.log itself stays tracked -- it is the run's readable record.
results/retrain_state.json
.gstack/

# Commit-pinned external Kronos checkout (see requirements-kronos.txt); not vendored.
src/external/kronos/upstream/

# Local working notes and task briefs — not part of the deliverable
.dev/
